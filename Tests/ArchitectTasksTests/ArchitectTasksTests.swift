import XCTest
@testable import ArchitectTasks

final class ArchitectTasksTests: XCTestCase {
    func testExample() throws {
        XCTAssertEqual(ArchitectTasks().text, "Hello, ArchitectTasks!")
    }
}
